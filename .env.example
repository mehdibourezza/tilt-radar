# Riot Games API
RIOT_API_KEY=RGAPI-your-key-here
RIOT_DEFAULT_REGION=europe          # europe | americas | asia
RIOT_DEFAULT_PLATFORM=euw1          # euw1 | na1 | kr | ...

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=tiltRadar
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres          # CHANGE in production

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# App
APP_ENV=development                 # development | production
SECRET_KEY=change-me-in-production  # CHANGE in production (use: python -c "import secrets; print(secrets.token_urlsafe(32))")
LOG_LEVEL=INFO

# W&B
WANDB_API_KEY=your-wandb-key
WANDB_PROJECT=tilt-radar
